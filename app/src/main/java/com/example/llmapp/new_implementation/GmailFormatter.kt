class GmailFormatter : OutputFormatter {
    override fun formatOutput(message: Message): String {
        // Implementation to format the output for Gmail
    }
}
// This is a placeholder implementation. In a real application, you would format the message according to Gmail's requirements.
// You might use HTML formatting or plain text, depending on the requirements of the Gmail API.